# 会話モード: rule (Phase 1 ルールベース) / ai (Phase 2 生成AI)
CONVERSATION_MODE=rule

# Phase 2 で使用（Phase 1 では不要）
AI_API_KEY=
AI_MODEL=

# 開発用モード: 内部的な判定・分岐・非共有情報のデバッグ表示を有効化
DEBUG_MODE=true

# SQLite データベースの保存先（省略時: ./data/kokoro.db）
# KOKORO_DB_PATH=
